
services:
  profile-anthropometric:
    build: .
    container_name: profile-anthropometric-service
    ports:
      - "8004:8004"
    volumes:
      - ./results:/app/results
      - ./models:/app/models:ro
    environment:
      - CUDA_VISIBLE_DEVICES=0
      - PYTHONPATH=/app
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8004/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # CPU-only version (uncomment if no GPU available)
  # profile-anthropometric-cpu:
  #   build: .
  #   container_name: profile-anthropometric-service-cpu
  #   ports:
  #     - "8004:8004"
  #   volumes:
  #     - ./results:/app/results
  #     - ./models:/app/models:ro
  #   environment:
  #     - CUDA_VISIBLE_DEVICES=-1
  #     - PYTHONPATH=/app
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:8004/health"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #     start_period: 40s

  # Enhanced version with both models
  # profile-anthropometric-enhanced:
  #   build: .
  #   container_name: profile-anthropometric-service-enhanced
  #   ports:
  #     - "8004:8004"
  #   volumes:
  #     - ./results:/app/results
  #     - ./models:/app/models:ro
  #     # Ensure both model files are available:
  #     # - best_point_detection_model_v2.pth (primary CNN model)
  #     # - facial_landmark_gnn.pth (optional GNN model)
  #   environment:
  #     - CUDA_VISIBLE_DEVICES=0
  #     - PYTHONPATH=/app
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:8004/health"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #     start_period: 40s
